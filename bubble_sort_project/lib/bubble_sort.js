function swap(array, idx1, idx2) {
    let temp = array[idx1]
    array[idx1] = array[idx2]
    array[idx2] = temp
    return array
}


function bubbleSort(array) {
    let sorted = true
    while (sorted) {
        sorted = false
        if( array[i] > array[i+1])
        
    }
}


module.exports = {
    bubbleSort,
    swap
};